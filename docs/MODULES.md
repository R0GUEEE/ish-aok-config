# Extended module index

- terminal_tools.sh: TERM, color, Unicode, title, tmux, screen, zellij
- file_managers.sh: nnn, lf, ranger, yazi, mc, tere
- search_tools.sh: ripgrep, fd, fzf, locate, bat, eza
- compression.sh: tar/gzip/xz/zstd/zip/7zip helpers and benchmark
- build_compilers.sh: Make, CMake, Meson, Cargo, GCC, Clang, TCC, ccache
- languages.sh: Python, Node, Rust, Go, Java, Swift
- network_tools.sh: DNS, HTTP, TLS, ports, routes, proxy, temporary server
- storage_tools.sh: usage, large files, duplicates, links, mounts, cleanup, benchmark
- logging_tools.sh: rsyslog, syslog-ng, BusyBox syslogd, logrotate
- security_tools.sh: permissions, SUID/SGID, account and integrity checks
- profiles_bundles.sh: system profiles and software bundles
- dotfiles_hooks.sh: import/export, compare, sync, Git and hook scripts
- rootfs_builder.sh: discovery, builders, validation, cloning, archiving, inventory
- diagnostics_full.sh: combined health and compatibility reports

## Menu additions

System Configuration now includes Init system and SysVinit plus Sudo/doas and admin privileges. User Environment includes Additional tool configuration. The main dashboard includes Project verbosity and behavior.

### mount_manager.sh

General and AOK-rootfs mount management, bind mounts, virtual filesystems, tmpfs, remounting, unmount diagnostics, fstab management, and reusable mount profiles.
